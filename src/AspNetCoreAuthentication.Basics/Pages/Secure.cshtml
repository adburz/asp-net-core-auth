@page
@model OpenIdSample.Web.Pages.SecureModel



<div>
    <h3>User claims</h3>

    <dl>
        @foreach(var claim in User.Claims)
        {
            <dt>@claim.Type</dt>
            <dt>@claim.Value</dt>
            <br />
        }
    </dl>
</div>